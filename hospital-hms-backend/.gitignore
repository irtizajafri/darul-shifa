node_modules
# Keep environment variables out of version control
.env

/src/generated/prisma

# Uploaded patient documents (files live on this server's disk, not in git)
/uploads/*
!/uploads/.gitkeep
